# pip / conda 安装依赖（不含第三方 submodule 内的依赖）
# pydicom 3.x 已能读 JPEG2000 if 系统装了 OpenJPEG
# 不需要再装 pylibjpeg-openjpeg

pydicom>=3.0
numpy>=1.24
pillow>=9.0
tqdm>=4.65

# 第三方 submodule 由 git submodule 提供,各自 README 自带
# 但本项目自己用到的
torch>=2.1
timm>=0.9
ml-collections>=0.1.0      # TransPath R50-ViT-B_16 config
scipy>=1.10
scikit-learn>=1.3
requests>=2.28
matplotlib>=3.5
pandas>=1.5

# Optional (不强制)
# efficientnet-pytorch>=0.7    # DeepGEM step3 有用到,但项目主线用不到
# albumentations>=1.3          # DeepGEM step3 train pipeline 里的
# rich>=13                     # DeepGEM step3 用 progress bar
